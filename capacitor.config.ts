import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.soulfire.production',
  appName: 'soulfire-chronicles-production',
  webDir: 'public'
};

export default config;
